package com.fluixcrm.crm

import io.flutter.embedding.android.FlutterActivity

// Activity principal para la app Android.
// Se crea explícitamente en el paquete `com.fluixcrm.crm` para
// coincidir con el `namespace`/`applicationId` declarado en Gradle
// y evitar ClassNotFoundException al arrancar la app.
class MainActivity: FlutterActivity()


import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:planeag_flutter/features/dashboard/pantallas/pantalla_dashboard.dart';

void main() {
  testWidgets('Dashboard carga widgets principales', (WidgetTester tester) async {
    // Requires mocking providers/services because Dashboard likely calls them on init
    // Skipping implementation details for brevity, assuming mock setup
  });
}


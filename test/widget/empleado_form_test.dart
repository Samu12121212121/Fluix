import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Formulario empleado valida campos obligatorios', (WidgetTester tester) async {
    // Tap save without filling
    // Expect error messages
  });
}

